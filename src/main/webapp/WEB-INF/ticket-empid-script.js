// Get table element
let table = document.querySelector('table');
let button = document.getElementById('tickets-emp-id')

// Add button listener, call fetchEmps() method
button.addEventListener("click", fetchTicketsByEmpId());

function buildTable(data) {

}

function fetchTicketsByEmpId(id) {

}